#version 400 core
#include "SharedDefines.h"
PROG_VERTEX_SHADER(PROG_ATOM)

out vec2 varUv;

void main()
{
	//vec4 tAlbedo = texture2D(texAlbedo, varUv);
	//tAlbedo.r = 0;
	//tAlbedo.b = 1;
	//tAlbedo.g = 0;
	//fragColor = tAlbedo;
	//if(fragColor.a < 0.5)
	//	discard;
}
